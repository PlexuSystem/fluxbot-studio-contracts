# Versioning

- Se usa semver para releases del repo de contratos.
- Cambios compatibles incrementan `minor`.
- Cambios de corrección sin afectar shape incrementan `patch`.
- Cambios incompatibles incrementan `major`.

La versión de API HTTP actual se mantiene en `info.version` del OpenAPI.
